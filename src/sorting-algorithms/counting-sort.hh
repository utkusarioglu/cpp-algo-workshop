#ifndef SORTING_ALGORITHMS_COUNTING_SORT_HH
#define SORTING_ALGORITHMS_COUNTING_SORT_HH

#include <vector>

class CountingSort
{
public:
  std::vector<int> loop(const std::vector<int> &unsorted);
};

#endif
